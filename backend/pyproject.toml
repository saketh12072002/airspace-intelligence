[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]
asyncio_default_fixture_loop_scope = "function"

[project]
name = "airspace-intelligence"
version = "0.1.0"
description = "Airspace Intelligence Backend API"
authors = [{name = "Developer"}]
requires-python = ">=3.10"
